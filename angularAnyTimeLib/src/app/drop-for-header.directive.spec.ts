import { DropForHeaderDirective } from './drop-for-header.directive';

describe('DropForHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DropForHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
